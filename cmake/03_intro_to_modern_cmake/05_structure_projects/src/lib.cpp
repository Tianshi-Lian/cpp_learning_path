#include <project/lib.h>

#include <tuple>
#include <vector>
#include <algorithm>

std::tuple<double, double> accumulate_vector(const std::vector<double>& values) {
    return std::make_tuple(5.0, 8.9);
}

